name: Validate Arch PKGBUILDs

on: [push, pull_request]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
      - run: bash build_scripts/validate-arch-pkgbuilds.sh
